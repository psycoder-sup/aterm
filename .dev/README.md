# .dev

Scratch space for local development — throwaway scripts, one-off experiments, reproduction cases, notes.

- `tmp/` — gitignored. Put temporary files here instead of `/tmp` so they stay co-located with the project.
- Anything else in `.dev/` is tracked; commit it if it's worth keeping.
