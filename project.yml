name: aterm
options:
  bundleIdPrefix: com.aterm
  deploymentTarget:
    macOS: "26.0"
  xcodeVersion: "26.3"
  minimumXcodeGenVersion: "2.0"

packages:
  swift-argument-parser:
    url: https://github.com/apple/swift-argument-parser
    from: "1.5.0"
  TOMLKit:
    url: https://github.com/LebJe/TOMLKit
    from: "0.6.0"

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    MACOSX_DEPLOYMENT_TARGET: "26.0"
    ARCHS: arm64

schemes:
  aterm:
    build:
      targets:
        aterm: all
        aterm-cli: all
        atermTests: [test]
        atermUITests: [test]
    test:
      targets:
        - atermTests
        - atermUITests

targets:
  aterm:
    type: application
    platform: macOS
    sources:
      - path: aterm
        excludes:
          - "Vendor/**"
          - "Resources/claude"
          - "Resources/aterm-hook-log.sh"
          - "Resources/aterm-claude-settings.json"
          - "Resources/shell-integration/**"
    dependencies:
      - target: aterm-cli
      - package: TOMLKit
        product: TOMLKit
    postCompileScripts:
      - script: cp -f "$BUILT_PRODUCTS_DIR/aterm-cli" "$BUILT_PRODUCTS_DIR/${PRODUCT_NAME}.app/Contents/MacOS/aterm-cli"
        name: Bundle aterm-cli
        inputFiles:
          - $(BUILT_PRODUCTS_DIR)/aterm-cli
        outputFiles:
          - $(BUILT_PRODUCTS_DIR)/$(PRODUCT_NAME).app/Contents/MacOS/aterm-cli
      - script: |
          SHARE="$SRCROOT/.ghostty-src/zig-out/share"
          RESOURCES="$BUILT_PRODUCTS_DIR/${PRODUCT_NAME}.app/Contents/Resources"
          rsync -a --delete "$SHARE/ghostty/" "$RESOURCES/ghostty/"
          rsync -a --delete "$SHARE/terminfo/" "$RESOURCES/terminfo/"
        name: Bundle Ghostty Resources
        inputFiles:
          - $(SRCROOT)/.ghostty-src/zig-out/share/ghostty/
          - $(SRCROOT)/.ghostty-src/zig-out/share/terminfo/
        outputFiles:
          - $(BUILT_PRODUCTS_DIR)/$(PRODUCT_NAME).app/Contents/Resources/ghostty/
          - $(BUILT_PRODUCTS_DIR)/$(PRODUCT_NAME).app/Contents/Resources/terminfo/
      - script: |
          cp -f "$SRCROOT/aterm/Resources/claude" "$BUILT_PRODUCTS_DIR/${PRODUCT_NAME}.app/Contents/Resources/claude"
          chmod +x "$BUILT_PRODUCTS_DIR/${PRODUCT_NAME}.app/Contents/Resources/claude"
        name: Bundle Claude Wrapper
        inputFiles:
          - $(SRCROOT)/aterm/Resources/claude
        outputFiles:
          - $(BUILT_PRODUCTS_DIR)/$(PRODUCT_NAME).app/Contents/Resources/claude
      - script: cp -f "$SRCROOT/aterm/Resources/aterm-claude-settings.json" "$BUILT_PRODUCTS_DIR/${PRODUCT_NAME}.app/Contents/Resources/aterm-claude-settings.json"
        name: Bundle Claude Settings
        inputFiles:
          - $(SRCROOT)/aterm/Resources/aterm-claude-settings.json
        outputFiles:
          - $(BUILT_PRODUCTS_DIR)/$(PRODUCT_NAME).app/Contents/Resources/aterm-claude-settings.json
      - script: |
          cp -f "$SRCROOT/aterm/Resources/aterm-hook-log.sh" "$BUILT_PRODUCTS_DIR/${PRODUCT_NAME}.app/Contents/Resources/aterm-hook-log.sh"
          chmod +x "$BUILT_PRODUCTS_DIR/${PRODUCT_NAME}.app/Contents/Resources/aterm-hook-log.sh"
        name: Bundle Claude Hook Logger
        inputFiles:
          - $(SRCROOT)/aterm/Resources/aterm-hook-log.sh
        outputFiles:
          - $(BUILT_PRODUCTS_DIR)/$(PRODUCT_NAME).app/Contents/Resources/aterm-hook-log.sh
      - script: |
          SI_SRC="$SRCROOT/aterm/Resources/shell-integration"
          SI_DST="$BUILT_PRODUCTS_DIR/${PRODUCT_NAME}.app/Contents/Resources/shell-integration"
          mkdir -p "$SI_DST/zsh"
          # Copy zsh dotfiles (renamed from non-dot source to dotfile in bundle)
          for f in zshenv zprofile zshrc zlogin; do
            cp -f "$SI_SRC/zsh/$f" "$SI_DST/zsh/.$f"
          done
          # Copy integration scripts
          cp -f "$SI_SRC/aterm-zsh-integration.zsh" "$SI_DST/aterm-zsh-integration.zsh"
          cp -f "$SI_SRC/aterm-bash-integration.bash" "$SI_DST/aterm-bash-integration.bash"
        name: Bundle Shell Integration
        inputFiles:
          - $(SRCROOT)/aterm/Resources/shell-integration/zsh/zshenv
          - $(SRCROOT)/aterm/Resources/shell-integration/zsh/zprofile
          - $(SRCROOT)/aterm/Resources/shell-integration/zsh/zshrc
          - $(SRCROOT)/aterm/Resources/shell-integration/zsh/zlogin
          - $(SRCROOT)/aterm/Resources/shell-integration/aterm-zsh-integration.zsh
          - $(SRCROOT)/aterm/Resources/shell-integration/aterm-bash-integration.bash
        outputFiles:
          - $(BUILT_PRODUCTS_DIR)/$(PRODUCT_NAME).app/Contents/Resources/shell-integration/zsh/.zshenv
          - $(BUILT_PRODUCTS_DIR)/$(PRODUCT_NAME).app/Contents/Resources/shell-integration/aterm-zsh-integration.zsh
          - $(BUILT_PRODUCTS_DIR)/$(PRODUCT_NAME).app/Contents/Resources/shell-integration/aterm-bash-integration.bash
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.aterm.app
        PRODUCT_NAME: aterm
        INFOPLIST_FILE: ""
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_CFBundleDisplayName: aterm
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.developer-tools
        INFOPLIST_KEY_UTExportedTypeDeclarations: |
          (
            {
              UTTypeIdentifier = "com.aterm.tab-drag-item";
              UTTypeConformsTo = ("public.data");
              UTTypeDescription = "aterm Tab";
              UTTypeTagSpecification = {};
            },
            {
              UTTypeIdentifier = "com.aterm.space-drag-item";
              UTTypeConformsTo = ("public.data");
              UTTypeDescription = "aterm Space";
              UTTypeTagSpecification = {};
            },
            {
              UTTypeIdentifier = "com.aterm.workspace-drag-item";
              UTTypeConformsTo = ("public.data");
              UTTypeDescription = "aterm Workspace";
              UTTypeTagSpecification = {};
            }
          )
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: aterm/aterm.entitlements
        ENABLE_APP_SANDBOX: false
        SWIFT_OBJC_BRIDGING_HEADER: aterm/aterm-Bridging-Header.h
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/aterm/Vendor/GhosttyKit.xcframework/macos-arm64/Headers
        LIBRARY_SEARCH_PATHS:
          - $(SRCROOT)/aterm/Vendor/GhosttyKit.xcframework/macos-arm64
        OTHER_LDFLAGS:
          - -lghostty-fat
          - -lc
          - -lc++
          - -framework Carbon
      configs:
        Debug:
          PRODUCT_BUNDLE_IDENTIFIER: com.aterm.app.debug
          PRODUCT_NAME: aterm-debug
          PRODUCT_MODULE_NAME: aterm
          INFOPLIST_KEY_CFBundleName: Aterm (debug)
          INFOPLIST_KEY_CFBundleDisplayName: Aterm (debug)

  atermTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: atermTests
      - path: aterm-cli/CommandLogger.swift
    dependencies:
      - target: aterm
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.aterm.tests
        GENERATE_INFOPLIST_FILE: true
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/aterm.app/Contents/MacOS/aterm
        BUNDLE_LOADER: $(TEST_HOST)
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/aterm/Vendor/GhosttyKit.xcframework/macos-arm64/Headers
      configs:
        Debug:
          TEST_HOST: $(BUILT_PRODUCTS_DIR)/aterm-debug.app/Contents/MacOS/aterm-debug

  aterm-cli:
    type: tool
    platform: macOS
    sources:
      - path: aterm-cli
    dependencies:
      - package: swift-argument-parser
        product: ArgumentParser
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.aterm.cli
        PRODUCT_NAME: aterm-cli

  atermUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - path: atermUITests
    dependencies:
      - target: aterm
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.aterm.uitests
        GENERATE_INFOPLIST_FILE: true
